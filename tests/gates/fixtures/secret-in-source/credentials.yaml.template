# Template. The guardrail test substitutes the placeholders with a
# credential-shaped value at runtime, in a directory outside this repository.
#
# The repository therefore contains no credential at all — not a real one, not a
# fabricated one. See README.md for why that matters.
aws_access_key_id: __AWS_ACCESS_KEY_ID__
aws_secret_access_key: __AWS_SECRET_ACCESS_KEY__
region: eu-central-1
